pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        sh '''#!/bin/bash
                 echo "pipeline practice"
                 pwd
                 ls -ltr
         '''
      }
    }
  }
}
