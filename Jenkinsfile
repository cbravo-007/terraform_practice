pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        bash '''#!/bin/bash
                 echo "pipeline practice"
                 pwd
                 ls -ltr
         '''
      }
    }
  }
}
